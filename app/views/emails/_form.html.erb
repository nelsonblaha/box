<%= form_for(@email) do |f| %>
  <% if @email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% @email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
<!--   <div class="field">
    <%= f.label :rank %><br />
    <%= f.number_field :rank %>
  </div> -->
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :class => "ya", :cols => 100, :rows => 10 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
